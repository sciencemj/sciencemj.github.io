/* Writing entries.
   The single entry below is a PLACEHOLDER — fake title, date and url. Replace it
   with a real post (or empty the array) before this goes public.

   Same shape as projects.data.js: the array body stays valid JSON.
   `url` can point anywhere — this repo, another repo, or an external blog — so the
   "same repo vs. separate repo + redirect" decision can be made later without
   changing the page. */
window.PORTFOLIO_POSTS = [
  {"date":"2026-07-12","kind":"Post","title":"Dummy post — replace with a real one","url":"https://github.com/sciencemj","tags":["Placeholder"]}
];
